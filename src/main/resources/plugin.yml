name: CraftBookPipePredicates
main: me.blvckbytes.craft_book_pipe_predicates.CraftBookPipePredicatesPlugin
version: 0.0.3
author: BlvckBytes
api-version: 1.14
depend: [CraftBook, ItemPredicateParser, ProtocolLib]
softdepend: [floodgate]
commands:
  pipepredicate:
  pipesearch:
  pipeteleport:
permissions:
  craftbookpipepredicates.command.pipepredicate.reload:
    description: 'Reload the plugin''s configuration'
    default: op
  craftbookpipepredicates.command.pipepredicate.search:
    description: 'Search the whole pipe using predicates'
    default: op
  craftbookpipepredicates.command.pipepredicate.capacities:
    description: 'Analyze the remaining capacities of a pipe''s containers'
    default: op
  craftbookpipepredicates.command.pipepredicate.locate-predicates:
    description: 'Search for predicates in a pipe via string-contains'
    default: op
  craftbookpipepredicates.auto-init-signs:
    description: 'Automatically initialize blank signs to a valid pipe sign'
    default: op